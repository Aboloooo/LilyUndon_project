
user with security level have access to only timeslots page 

timeslot page must contain an extra btn to print the entire page

if a session is terminated user must land into index page instead of ...

site selection shouldnt move center if print btn is disabled;

JavaScript stings, add_user.php needs translation